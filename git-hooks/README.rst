****************************************
git hooks
****************************************


To add a hook, add the file to peregrine/git-hooks/. 
It will be sym-linked to `.git/hooks/` upon running
`deps.sh`.

